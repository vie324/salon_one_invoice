-- =========================================================================
-- 0017: 自社情報(請求書の発行元) — インボイス登録番号と振込先の整備
--
--   1) organizations.bank_branch_code: 支店番号(3桁)。請求書の【お振込先】に併記する。
--   2) 適格請求書発行事業者の登録番号を初期投入する。
--      インボイス制度では、適格請求書に登録番号の記載が必要となるため、
--      未設定(空)のときだけサロンワンの登録番号で埋める。
--      ※ 既に値が入っている場合は上書きしない(設定画面からの編集を優先)。
-- =========================================================================

alter table organizations add column if not exists bank_branch_code text;

update organizations
   set registration_number = 'T2010801037576'
 where coalesce(registration_number, '') = '';
